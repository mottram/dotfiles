# File: ~/.zshrc
# Version: 0.3
# Author: Jack Mottram <j@ck.mottr.am>
# TODO: tidy up aliases & functions, use checks.
# TODO: sort out completion - bit iffy at the moment.
# TODO: move path & exports?
source $HOME/dotfiles/zsh/paths.zsh
source $ZSH_DIR/checks.zsh
source $ZSH_DIR/functions.zsh
source $ZSH_DIR/aliases.zsh
source $ZSH_DIR/history.zsh
source $ZSH_DIR/bindkeys.zsh
source $ZSH_DIR/applications.zsh
source $ZSH_DIR/completions.zsh
source $ZSH_DIR/plugins.zsh
source $ZSH_DIR/prompt.zsh

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh


# BOOKMARKS_FILE="$HOME/.named_directories"

# function bm() {
#     echo "export $1=$(pwd)" >> $BOOKMARKS_FILE
#     source $BOOKMARKS_FILE
# }

# alias bmcd='cd $(cut -d= -f2- .named_directories | fzf)'
