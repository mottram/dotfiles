### Functions

# Quick git commit
function gg {
  git commit -v -a -m "$*"
}

# View source files with pygments syntax colouring
function pless {
  pygmentize $1 | less -r
}

# mkdir, cd into it
function mkcd {
  mkdir $1
  cd $1
}

# list and grep
function lgr {
  ls -a | grep $1 
}
